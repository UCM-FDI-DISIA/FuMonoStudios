#pragma once
#include "src/sdlutils/Texture.h"
/*
using uint = unsigned int;
class Character
{
private:
	Texture* myTexture;
	uint felicidad;

public:
	Character();
	Character(Texture * t, uint f);
	~Character();

	//getters && setters
	uint getFelicidad() { return felicidad;};
	Texture* getTexture() { return myTexture; };
};
*/
